import React from "react";

const Login = (props) => {
    return(
        <div>
            <main>
                <textarea>...Some Text</textarea>
            </main>
        </div>
    );
};

export default Login;